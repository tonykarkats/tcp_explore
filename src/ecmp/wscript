# -*- Mode: python; py-indent-offset: 4; indent-tabs-mode: nil; coding: utf-8; -*-

# def options(opt):
#     pass

# def configure(conf):
#     conf.check_nonfatal(header_name='stdint.h', define_name='HAVE_STDINT_H')

def build(bld):
    module = bld.create_ns3_module('ecmp', ['core'])
    module.source = [
        'model/ecmp-channel.cc',
        'model/ecmp-net-device.cc',
        'helper/ecmp-helper.cc',
        ]

    module_test = bld.create_ns3_module_test_library('ecmp')
    module_test.source = [
        'test/ecmp-test-suite.cc',
        ]

    headers = bld(features='ns3header')
    headers.module = 'ecmp'
    headers.source = [
        'model/ecmp-channel.h',
        'model/ecmp-net-device.h',
        'helper/ecmp-helper.h',
        ]

    if bld.env.ENABLE_EXAMPLES:
        bld.recurse('examples')

    # bld.ns3_python_bindings()

